# Core dependencies
flask>=3.0.3
flask-cors>=4.0.1
python-dotenv>=1.0.1

# LangChain & LangGraph
langchain>=0.3.7
langchain-core>=0.3.15
langchain-community>=0.3.5
langchain-text-splitters>=0.3.2
langchain-openai>=0.3.0
langgraph>=0.2.45
langgraph-checkpoint>=2.0.0
langgraph-sdk>=0.1.35

# Vector Database
chromadb>=0.5.18

# Database
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.35

# Document Conversion (Primary: MarkItDown)
markitdown[all]>=0.1.4

# PDF Processing (fallback + utilities)
pypdf>=4.3.1
PyPDF2>=3.0.0
pdf2image>=1.17.0
Pillow>=10.0.0

# Image Analysis (stamp/seal color detection)
opencv-python>=4.8.0
numpy>=1.24.0

# Office Document Processing (Word, Excel, PowerPoint)
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=0.6.23

# Windows-only: Legacy Office formats (.doc, .xls, .ppt)
# Uncomment on Windows if needed:
# pywin32>=306

# Chainlit UI (Primary - Single-App Architecture)
chainlit>=2.9.6

# Gradio UI (Legacy - for Sub-Agent interface)
gradio>=4.44.0

# LiteLLM (Multi-provider LLM abstraction)
litellm==1.81.15

# OpenAI (for Vision API)
openai>=1.50.0

# Anthropic (for Claude Vision)
anthropic>=0.34.0

# Google Generative AI (for Gemini)
google-generativeai>=0.8.0

# Local LLM
ollama>=0.3.3

# Web Search (for /web command — ddgs is the actively maintained package)
ddgs>=9.0.0

# Encryption
cryptography>=41.0.0

# Security
defusedxml>=0.7.1

# Utilities
python-multipart>=0.0.17
aiofiles>=24.1.0
pydantic>=2.9.2
httpx[http2]>=0.27.0
h2>=4.0.0

# Dev
pytest>=8.3.3
pytest-asyncio>=0.24.0

# Arize Phoenix - LLM Observability (v3.4.0)
arize-phoenix>=9.0.0
arize-phoenix-otel>=0.8.0
openinference-instrumentation-litellm>=0.1.18

# ============================================================
# Phase 2A new dependencies
# ============================================================

# Document parsing engine (PDF table structure, layout analysis) — Tier 3 last resort
docling>=2.0.0

# OCR Tier 0: PDF native text layer extraction (no ML, no GPU)
pymupdf>=1.24.0

# OCR Tier 1: Multi-language OCR, 32 countries, CPU-only (no CUDA dependency)
easyocr>=1.7.0

# Knowledge graph RAG (cross-document semantic search + entity relations)
lightrag-hku>=1.4.8

# Embedding fallback (auto-downloaded ~420MB on first run when Ollama unavailable)
# sentence-transformers>=3.0.0
# torch>=2.0.0

# Server mode only (required only when QMS_MODE=server)
# celery>=5.3.0
# redis>=5.0.0
# flower>=2.0.0
# bcrypt>=4.0.0
