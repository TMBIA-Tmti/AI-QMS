[project]
# AI-QMS Phase 1 Document Control System
# Version: v3.6.0 (Chainlit)

# List of environment variables to be provided by each user to use the app.
user_env = []

# Duration (in seconds) during which the session is saved when the connection is lost
# 999999 = no practical limit — keeps session alive indefinitely
session_timeout = 999999

# Enable third parties caching
cache = false

# Authorized origins — include fallback ports 3001-3005 for watchdog restarts
allow_origins = [
    "http://localhost:3000", "http://127.0.0.1:3000",
    "http://localhost:3001", "http://127.0.0.1:3001",
    "http://localhost:3002", "http://127.0.0.1:3002",
    "http://localhost:3003", "http://127.0.0.1:3003",
    "http://localhost:3004", "http://127.0.0.1:3004",
    "http://localhost:3005", "http://127.0.0.1:3005",
]

# Follow symlink for asset mount (needed for junction at C:\Users\MDR\AI-QMS)
follow_symlink = true

[features]
# Process and display HTML in messages
unsafe_allow_html = false

# Process and display mathematical expressions
latex = false

# Autoscroll new user messages
user_message_autoscroll = true

# Automatically tag threads with the current chat profile
auto_tag_thread = true

# Allow users to edit their own messages
edit_message = false

# Authorize users to spontaneously upload files with messages
[features.spontaneous_file_upload]
    enabled = true
    accept = ["application/pdf", "application/msword", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", "application/vnd.ms-excel", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "application/vnd.ms-powerpoint", "application/vnd.openxmlformats-officedocument.presentationml.presentation", "image/*", "text/*"]
    max_files = 10
    max_size_mb = 50

[UI]
# Name of the assistant
name = "AI-QMS"

# Default theme
default_theme = "light"

# Chainlit frontend UI language (placeholder, buttons, sidebar, watermark).
# If not set, the browser's language is used. This is independent of the
# app-level i18n (Eira messages) controlled by the Language selector.
# language = "zh-TW"

# Chain of Thought display mode
cot = "hidden"

layout = "wide"

# Connection resilience: auto-reconnect JS injected into every page
custom_js = "/public/reconnect.js"

[meta]
generated_by = "1.3.0"
